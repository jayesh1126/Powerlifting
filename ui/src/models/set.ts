export interface Set {
    // All types string because it will come from JSON
    _id: string,
    userId: string,
    exerciseName: string,
    weight: string,
    repetitions: string,
    rpe: string,
    date: string,
}